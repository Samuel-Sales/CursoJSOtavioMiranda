/**
 * Aritmeticos
 * + Adicao / Concatenacao
 * - / * **
 * % Resto da divisao
 */

const num1 = 5;
const num2 = 10;
const num3 = '10';
console.log(num1 ** num2);

let contador = 1;
contador++;
console.log(++contador);

// NaN - Not a number, 
// parseInt(inteiro), parseFloat(decimais)
const numero = 10;
const numero2 = parseInt('8'); // Convertendo de string para INT